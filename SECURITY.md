# Security

Report security issues privately through the repository owner.

This module does not require provider credentials. Tests and benchmarks must use
temporary homes, targets, package directories, and stub executables. The setup
manager refuses implicit live Pi configuration paths and strips common provider
API key variables from managed launches.
